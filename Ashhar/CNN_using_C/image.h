struct Image{
    // Declare a 2D array to store the image data.
    float image_array[30][30];
    int height;
    int width;
};

struct Patch{
    // Declare a 2D array to store the image data.
    float image_array[3][3];
    int height;
    int width;
};