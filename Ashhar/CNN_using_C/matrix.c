#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

struct Matrix(
    int rows;
    int columns;
    float **elements;
)

void Matrix_Init(Matrix* x, int r, int c){

    for(int i = 0; i < x->r; i++){
        for(int j = 0; j < x->c; j++){
            
        }
    }
}